import { createTheme } from "@mui/material";

export const globalTheme = createTheme({
  palette: {
    primary: {
      main: "#FCB408",
      dark: "#EBA500",
      contrastText: "#fff",
      light: "#FFFFFF",
    },
  },
});
